# Generated by `genesis-cc` (and by `--verify`, which emits before it compiles): the
# `.genesis` document is the source of truth, so the emitted class is a build byproduct.
samples/*.h
samples/*.cpp
